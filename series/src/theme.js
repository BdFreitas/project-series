import { createTheme } from "@mui/system";

const theme = createTheme({
    palatte: {
        otherColor : {
            main: "#DA70D6",
            light: "#DA70D6",
            dark: "black" 
        }
    }
})

export default theme;